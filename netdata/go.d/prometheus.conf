# Scrape the Spring Boot app's Micrometer/Prometheus endpoint (Actuator management port).
jobs:
  - name: spring_app
    # retry startup failures (e.g. netdata comes up before the app finishes booting)
    autodetection_retry: 60
    url: http://app:8081/actuator/prometheus
